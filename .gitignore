# state / secrets (agent local, never commit)
config.yaml
*.db
geo-cache.json
banned.json
notify.txt
github-outbox.jsonl
__pycache__/
*.pyc
.venv/
venv/
.env

# Agent local run stats (not the public stats.json built by CI)
/stats.jsonl

# Note: blocklist.txt / blocklist.iptables / blocklist.nftables / stats.json /
# threats.json at repo root are BUILT by .github/workflows/aggregate.yml and
# ARE committed (they are what GitHub Pages serves).
